"""Console output formatting.

Note: scanner.py currently uses its own inline output functions.
This module is reserved for future refactoring when output formatting
becomes more complex (e.g., color support, verbose mode).
"""
